<!-- 1.错误的写法 -->
<!-- <view>{{25.666666.toFixed(2)}}</view> -->
<!-- <view>{{25.6666666}}</view> -->

<!-- 2.wxs的定义方法 -->
<!-- 2.1直接在wxml中定义 wxs不能用es6的语法，只能用es5的语法 -->
<!-- <wxs module="info">
  var message = "Hello World";
  var name = "vgri";

  function sum(num1, num2) {
    return num1 + num2
  }

    module.exports = {
    message: message,
    name: name,
    sum: sum
  }
</wxs> -->

<!-- 2.2定义在单独的wxs文件中，再使用wxs标签进行导入 -->
<!-- 不能使用绝对路径，必须使用相对路径 -->
<wxs src="../../wxs/info.wxs" module="info"/>

<view>{{info.message}}</view>
<view>{{info.name}}</view>
<view>{{info.sum(20, 30)}}</view>

<wxs src="../../wxs/format.wxs" module="format"/>

<view>{{format.priceFormat(price)}}</view>
<view>{{format.priceFormat(price, 4)}}</view>
<view>{{format.dateFormat(time, 'yyyy-MM-dd hh:mm:ss')}}</view>
<view>{{date}}</view>